export default defineNuxtRouteMiddleware(() => {
  // const user = useSupabaseUser();
  // console.log("middlware", user.value, from, to);
  // if (!user.value) {
  //   return navigateTo("/");
  // }
  // return navigateTo("/dashboard");
});
